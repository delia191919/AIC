services:
  backend:
    build: .
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mirciulica007.go.ro:22050/aic_avalanse
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: changeme
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    ports:
      - "443:443"
      - "8081:80"
    depends_on:
      - backend
